/*예제 7번과 같이 1~9까지의 입력 받은 정수의 개수를 저장한 후
입력 받은 수의 모든 합을 구하는 프로그램을 작성하시오.
단, 1~9의 범위를 벗어나면 종료하고 아래와 같이 출력한다.*/
예시 입력 1 
4 9 8 2 4 2 4 22
예시 출력 1
2 : 2개
4 : 3개
8 : 1개
9 : 1개
모든 수의 합 : 33
*/

#include <stdio.h>

int main()
{
    int a[9]={0};//배열 선언과 동시에 초기화
    int n;
    int i;
    int sum=0;
    while(1){
        scanf("%d", &n);
        if (n<1||n>9) break;
        a[n-1]++;//n이 입력된 횟수가 곧 배열 n-1인덱스의 값이 됨.
    }
    for(i=0;i<9;i++){
        if(a[i]!=0) {
            printf("%d : %d개\n", i+1, a[i]);
            sum+=(i+1)*a[i];
        }
    }
    printf("모든 수의 합 : %d", sum);
    return 0;
}
